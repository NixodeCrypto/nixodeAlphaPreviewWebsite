import space, { SpaceProps } from '@/system/space';
import color, { ColorProps } from '@/system/color';
import typography, { TypographyProps } from '@/system/typography';
import layout, { LayoutProps } from '@/system/layout';
import flexbox, { FlexboxProps } from '@/system/flexbox';
import grid, { GridProps } from '@/system/grid';
import background, { BackgroundProps } from '@/system/background';
import border, { BorderProps } from '@/system/border';
import position, { PositionProps } from '@/system/position';
import shadow, { ShadowProps } from '@/system/shadow';
import cssTransform from '@/system/cssTransform';
import responsiveStyles from '@/system/responsiveStyles';
import compose from '@/system/compose';
import variant from '@/system/variant';

export {
  cssTransform,
  responsiveStyles,
  compose,
  variant,
  space,
  color,
  typography,
  layout,
  flexbox,
  grid,
  background,
  border,
  position,
  shadow,
};

export type {
  SpaceProps,
  ColorProps,
  TypographyProps,
  LayoutProps,
  FlexboxProps,
  GridProps,
  BackgroundProps,
  BorderProps,
  PositionProps,
  ShadowProps,
};
